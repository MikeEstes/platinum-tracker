import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#eee',
    marginHorizontal: 5,
    marginBottom: 10,
    padding: 10,
    borderRadius: 15,
  },
  trophyContainer: {
    flexDirection: 'row',
  },
});

export default styles;
